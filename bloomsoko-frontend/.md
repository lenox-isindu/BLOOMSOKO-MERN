Bloomsoko is a full-stack MERN e-commerce platform for flower and agricultural products, connecting growers directly with customers. The platform supports both ready products and pre-order/growing products.

 Architecture
Tech Stack
Frontend: React.js with React Router

Backend: Node.js, Express.js

Database: MongoDB with Mongoose ODM

Authentication: JWT (JSON Web Tokens)

File Storage: Cloudinary for image uploads

Payments: Paystack integration

Styling: CSS Modules + Custom CSS

 Project Structure
text
bloomsoko/
├── bloomsoko-backend/
│   ├── models/
│   │   ├── User.js
│   │   ├── Product.js
│   │   ├── Category.js
│   │   ├── Order.js
│   │   ├── Promotion.js
│   │   └── Analytics.js
│   ├── routes/
│   │   ├── auth.js
│   │   ├── products.js
│   │   ├── orders.js
│   │   ├── categories.js
│   │   ├── promotions.js
│   │   └── admin/
│   ├── controllers/
│   ├── middleware/
│   │   └── auth.js
│   └── server.js
├── bloomsoko-frontend/
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── context/
│   │   ├── services/
│   │   └── styles/
│   └── public/
└── .env
User Roles & Authentication
User Types
Customers - Regular users who browse and purchase products

Administrators - Manage products, orders, users, and promotions

Authentication Flow
JWT tokens stored in localStorage

Protected routes for authenticated users

Admin-specific middleware for admin routes

 Core Features
Product Management
Product Types: Ready, Growing, Pre-order

Inventory Tracking: Stock, reserved stock, low stock alerts

Product Flags: Featured, New, Limited, Best Seller, etc.

Categories: Hierarchical category system (Level 1, 2, 3)

Order System
Order Status: Pending, Completed, Cancelled

Payment Integration: Paystack for payments

Order Tracking: Full order history for users

Pickup Options: Multiple pickup station choices

Shopping Experience
Advanced Filtering: By category, price, flags, search

Product Booking: Pre-order for growing products

Shopping Cart: Persistent cart with inventory validation

Promotions: Banner, flash sales, seasonal offers

Admin Dashboard
Real-time Analytics: Orders, revenue, user statistics

Inventory Management: Low stock alerts, stock tracking

User Management: Customer accounts and order history

Promotion Management: Create and manage promotional campaigns

🗄️ Database Models
User Model
javascript
{
  firstName: String,
  lastName: String,
  email: String,
  phone: String,
  password: String,
  role: ['user', 'admin'],
  isActive: Boolean,
  lastLogin: Date
}
Product Model
javascript
{
  name: String,
  description: String,
  price: Number,
  category: ObjectId,
  inventory: {
    stock: Number,
    reservedStock: Number,
    lowStockThreshold: Number
  },
  productType: ['ready', 'growing', 'pre-order'],
  status: ['active', 'draft', 'archived'],
  flags: {
    isFeatured: Boolean,
    isNew: Boolean,
    onSale: Boolean,
    // ... more flags
  }
}
Order Model
javascript
{
  orderNumber: String,
  user: Mixed, // Can be ObjectId or string
  recipient: {
    firstName: String,
    lastName: String,
    email: String,
    phone: String
  },
  items: [{
    product: ObjectId,
    quantity: Number,
    price: Number,
    isBooking: Boolean
  }],
  totalAmount: Number,
  status: ['pending', 'completed', 'cancelled'],
  paymentStatus: ['pending', 'paid', 'failed']
}
Promotion Model
javascript
{
  title: String,
  description: String,
  type: ['banner', 'flash_sale', 'seasonal', 'special_offer'],
  position: ['top_banner', 'hero', 'sidebar', 'popup', 'product_page'],
  status: ['active', 'scheduled', 'inactive', 'expired'],
  startDate: Date,
  endDate: Date,
  discountType: ['percentage', 'fixed', 'free_shipping'],
  discountValue: Number
}
 API Endpoints
Authentication
POST /api/auth/register - User registration

POST /api/auth/login - User login

GET /api/auth/profile - Get user profile

PUT /api/auth/profile - Update profile

PUT /api/auth/change-password - Change password

Products
GET /api/products - Get all products (with filters)

GET /api/products/:id - Get single product

POST /api/admin/products - Create product (admin)

PUT /api/admin/products/:id - Update product (admin)

DELETE /api/admin/products/:id - Delete product (admin)

Orders
GET /api/orders/user - Get user orders

GET /api/orders/admin - Get all orders (admin)

POST /api/orders - Create order

PUT /api/orders/:id/status - Update order status (admin)

GET /api/orders/admin/stats - Order statistics (admin)

Promotions
GET /api/promotions/active - Get active promotions

GET /api/promotions - Get all promotions (admin)

POST /api/promotions - Create promotion (admin)

PUT /api/promotions/:id - Update promotion (admin)

Categories
GET /api/categories - Get all categories

POST /api/admin/categories - Create category (admin)

 Frontend Components
Key Components
Navbar - Navigation with auth state

ProductCatalog - Product listing with filters

ProductCard - Individual product display

ShoppingCart - Cart management

AdminDashboard - Admin analytics and management

PromotionsDisplay - Show active promotions

Pages
/ - Homepage with featured products

/products - Product catalog

/product/:id - Product details

/cart - Shopping cart

/checkout - Checkout process

/orders - User order history

/admin/* - Admin management panels

/promotions - Promotions page

🔧 Environment Variables
Backend (.env)
bash
MONGODB_URI=your_mongodb_connection_string
JWT_SECRET=your_jwt_secret
CLOUDINARY_CLOUD_NAME=your_cloudinary_name
CLOUDINARY_API_KEY=your_cloudinary_key
CLOUDINARY_API_SECRET=your_cloudinary_secret
PAYSTACK_SECRET_KEY=your_paystack_secret
Frontend (.env)
bash
REACT_APP_API_URL=http://localhost:5000/api
REACT_APP_PAYSTACK_PUBLIC_KEY=your_paystack_public_key


Deployment Guide
Backend Deployment
Set up MongoDB Atlas database

Configure environment variables

Deploy to hosting service (Render, Heroku, etc.)

Set up Cloudinary for file storage

Frontend Deployment
Build the React app: npm run build

Deploy to Netlify, Vercel, or similar

Configure environment variables

Set up proper redirects for React Router

🛠️ Development Setup
Backend
bash
cd bloomsoko-backend
npm install
npm run dev
Frontend
bash
cd bloomsoko-frontend
npm install
npm start
 Key Features Implementation
Inventory Management
Real-time stock tracking

Reserved stock for pending orders

Low stock alerts and notifications

Automatic stock status updates

Order Processing
Multi-step checkout process

Payment integration with Paystack

Order confirmation emails

Pickup station selection

Admin Analytics
Real-time dashboard with metrics

Sales trends and revenue tracking

User behavior insights

Stock level monitoring

 Workflow Examples
Customer Purchase Flow
Browse products → Add to cart → Checkout → Payment → Order confirmation

Admin Product Management
Create product → Set inventory → Add images → Publish → Monitor sales

Promotion Creation
Create promotion → Set dates/position → Configure discount → Activate → Track performance


Current Features
 User authentication system

 Product catalog with advanced filtering

 Shopping cart and checkout

Admin dashboard with analytics

 Promotion management system

 Inventory tracking and alerts

Order management system

Payment integration


 Advanced analytics with trends

 Email notifications system





acces the app on this link
https://bloomsoko.vercel.app/

at the moment use this for admin log in
lenox1739@gmail.com
12345678